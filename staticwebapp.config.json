{
  "routes": [
    {
      "route": "/api/upload/*",
      "methods": ["GET", "POST"],
      "rewrite": "/api/upload"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}